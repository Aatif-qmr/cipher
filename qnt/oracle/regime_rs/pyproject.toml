[build-system]
requires = ["maturin>=1.5,<2.0"]
build-backend = "maturin"

[project]
name = "regime-rs"
version = "0.1.0"
description = "Rust-backed LSTM regime inference — replaces PyTorch dependency"
requires-python = ">=3.10"
